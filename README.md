# Installation

Sur Ubuntu, s'assurer que vous avez les essentials :
```
sudo apt-get install -y build-essential
```

Ensuite : 
```
npm install
sudo npm start
```

Disponible sur :[localhost:3000](http://localhost:3000/).
